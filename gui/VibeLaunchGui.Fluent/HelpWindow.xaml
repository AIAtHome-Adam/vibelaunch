<ui:FluentWindow x:Class="VibeLaunchGui.Fluent.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Help" Height="720" Width="960"
        MinHeight="400" MinWidth="640"
        WindowBackdropType="Mica"
        ExtendsContentIntoTitleBar="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar x:Name="HelpTitleBar" Grid.Row="0" Title="Help" ShowMinimize="False" ShowMaximize="False" />

        <Grid Grid.Row="1" Margin="8">
            <wv2:WebView2 x:Name="HelpWebView"/>
            <TextBlock x:Name="LoadingText" Text="Loading help..."
                       HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.7"/>
        </Grid>
    </Grid>
</ui:FluentWindow>
